{
  "name": "knowledge-widget-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w apps/api",
    "dev:admin": "npm run dev -w apps/admin",
    "dev:widget": "npm run dev -w apps/widget",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
